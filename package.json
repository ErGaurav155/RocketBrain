{
  "name": "rocketbrain",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspace=dashboard",
    "start": "npm run start --workspace=dashboard",
    "lint": "npm run lint --workspace=dashboard",
    "type-check": "npm run type-check --workspace=dashboard"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
